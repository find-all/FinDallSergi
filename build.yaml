targets:
  $default:
    builders:
      json_serializable:
        options:
          explicit_to_json: true

global_options:
  freezed:freezed:
    runs_before:
      - isar_generator:isar_generator